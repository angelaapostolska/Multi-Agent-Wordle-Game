LLM integration result — MultiWord (sandbox verification run)

Command: python scenario_multiword.py --compare-llm --games 10 --seed 42 --no-results
Weights used: weights_multiword.npz (produced by this scenario's normal RL training,
which is completely unaffected by --llm/--llm-moderator — those flags are
demo-only).

IMPORTANT: this transcript was captured in a sandboxed environment with no
local Ollama server reachable, so every "[Ollama unavailable, using
template text — ...]" line is the code's designed fallback path, not a
bug — it proves the --llm/--llm-moderator/--compare-llm wiring runs
end-to-end without crashing. It does NOT contain real LLM-generated debate
text or a real LLM moderator vote (see the compare-llm result file: 0
usable votes were cast, by construction, since Ollama could not be
reached).

To capture the REAL result — actual model-generated arguments and a
non-zero LLM vote/override rate — run the same command on a machine with
Ollama installed and a model pulled (see README.md's "LLM Integration
Across All Scenarios" section), then replace the content below the line
of dashes with that output.

────────────────────────────────────────────────────────────────────────

[MultiWord] Building pattern matrix for 968 words...
[MultiWord] Pattern matrix ready.
Weights loaded ← weights_multiword.npz

[MultiWord] Comparing LLM moderator vs trained moderator over 10 games each...
(this makes real Ollama calls for the LLM-moderator half — expect it to take a while)
  [LLM-moderator   1/10] won  |    0.0s elapsed
  [LLM-moderator   2/10] won  |    0.0s elapsed
  [LLM-moderator   3/10] won  |    0.0s elapsed
  [LLM-moderator   4/10] won  |    0.1s elapsed
  [LLM-moderator   5/10] won  |    0.1s elapsed
  [LLM-moderator   6/10] won  |    0.1s elapsed
  [LLM-moderator   7/10] won  |    0.1s elapsed
  [LLM-moderator   8/10] won  |    0.1s elapsed
  [LLM-moderator   9/10] won  |    0.1s elapsed
  [LLM-moderator  10/10] won  |    0.1s elapsed
  LLM-moderator half done in 0.1s. Running the 10-game baseline (no Ollama calls, should be quick)...
  Baseline half done in 0.1s.
────────────────────────────────────────────────────────────
Turns played: 52  |  LLM cast a usable vote on 0 of them (Ollama unreachable/unparsed on 52)
Win rate WITH LLM moderator:      100.0%  (10/10)
Win rate baseline (trained only): 100.0%  (10/10)
────────────────────────────────────────────────────────────
